---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: argocd-application-controller
  namespace: monitoring

  labels:
    release: kube-prometheus-stack
    app.kubernetes.io/name: argocd-application-controller-monitor
    app.kubernetes.io/part-of: uptime-kuma-platform
    environment: dev
    managed-by: argocd

spec:
  namespaceSelector:
    matchNames:
      - argocd

  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-metrics

  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: argocd-server
  namespace: monitoring

  labels:
    release: kube-prometheus-stack
    app.kubernetes.io/name: argocd-server-monitor
    app.kubernetes.io/part-of: uptime-kuma-platform
    environment: dev
    managed-by: argocd

spec:
  namespaceSelector:
    matchNames:
      - argocd

  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-server-metrics

  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: argocd-repo-server
  namespace: monitoring

  labels:
    release: kube-prometheus-stack
    app.kubernetes.io/name: argocd-repo-server-monitor
    app.kubernetes.io/part-of: uptime-kuma-platform
    environment: dev
    managed-by: argocd

spec:
  namespaceSelector:
    matchNames:
      - argocd

  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-repo-server

  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: argocd-applicationset-controller
  namespace: monitoring

  labels:
    release: kube-prometheus-stack
    app.kubernetes.io/name: argocd-applicationset-controller-monitor
    app.kubernetes.io/part-of: uptime-kuma-platform
    environment: dev
    managed-by: argocd

spec:
  namespaceSelector:
    matchNames:
      - argocd

  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-applicationset-controller

  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s